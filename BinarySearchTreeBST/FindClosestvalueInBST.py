def BST(tree,target):
    #takes in tree and target
    #finds number within tree that is closest to target
    #tree is a tree if following
    # only valid if > all on left
    # <= all on right
    # children nodes either valid or None/null
    
